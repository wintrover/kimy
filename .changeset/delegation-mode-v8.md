---
"@moonshot-ai/kimi-code": minor
---

Add delegation mode (--orchestrator flag) with transaction isolation for sub-agents, including context snapshot/rollback, file ownership verification, and deterministic LLM parameter pinning.
