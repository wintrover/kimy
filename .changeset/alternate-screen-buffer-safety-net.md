---
"@moonshot-ai/kimi-code": minor
---

Use alternate screen buffer for the terminal UI to prevent scrollback accumulation, and add an exit safety net that reliably restores the terminal on unexpected failures.
