---
"@moonshot-ai/kimi-code": minor
---

Add render diagnostics system for investigating TUI duplicate rendering. Gated behind `KIMI_CODE_RENDER_DEBUG=1` env var, with `/render-log` slash command to dump event history as JSONL.
